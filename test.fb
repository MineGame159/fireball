struct Vec2 {
    x i32
    y i32
}

func main() i32 {
    var point = Vec2 {
        x: 1,
        y: 5
    };

    printf("X: %d, Y: %d\n", point.x, point.y);
    return 0;
}

extern func printf(format *u8, ...) void

extern func malloc(size u64) *void
extern func free(ptr *void) void
