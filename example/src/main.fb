struct Vec2 {
    x i32,
    y i32,
}

enum Animal {
    Dog = 9,
    Cat,
}

func main() i32 {
    for (var i = 0; i < 2; i += 1) {
        for (var j = 0; j < 3; j += 1) {
            printf("%d %d\n", i, j);
        }
    }

    return 0;
}
